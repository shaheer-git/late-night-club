# 🌙 Late Night Club

Community-powered app showing which places are **actually open late at night** — verified in real-time by people physically present.

> Open source · Self-hostable · No paywalls · MIT License

---

## Stack

| Layer     | Tech |
|-----------|------|
| Mobile    | React Native + Expo (Android / iOS / Web) |
| Styling   | NativeWind (Tailwind CSS) |
| Navigation| Expo Router v3 (file-based) |
| State     | Zustand |
| Maps      | react-native-maps + OpenStreetMap (free) |
| Routing   | OSRM public server (free) |
| Geocoding | Nominatim / OSM (free) |
| Backend   | FastAPI + PostgreSQL + PostGIS |
| Cache     | Redis |
| Jobs      | Celery |
| Auth      | JWT |
| Storage   | Cloudflare R2 (free tier) |

---

## Quick Start (Full Stack)

### 1. Clone & configure

```bash
git clone https://github.com/yourorg/late-night-club
cd late-night-club
cp .env.example backend/.env
```

### 2. Start everything with Docker

```bash
docker compose up
```

This starts:
- PostgreSQL + PostGIS on port 5432
- Redis on port 6379
- FastAPI backend on http://localhost:8000
- Celery worker + beat scheduler

### 3. Run DB migrations

```bash
docker compose exec backend alembic upgrade head
```

### 4. API docs

Open http://localhost:8000/docs (Swagger UI auto-generated by FastAPI)

---

## Mobile App

```bash
cd apps/mobile
npm install
npx expo start
```

- Press `a` for Android emulator
- Press `i` for iOS simulator
- Press `w` for web browser
- Scan QR with **Expo Go** app on your phone

---

## Project Structure

```
late-night-club/
├── apps/
│   └── mobile/              # Expo React Native app
│       ├── app/             # Expo Router screens
│       │   ├── (auth)/      # Login, Register, OTP, Welcome
│       │   ├── (tabs)/      # Home, Map, Add, Profile
│       │   ├── onboarding/  # City select, Location permission
│       │   ├── place/[id]   # Place detail
│       │   ├── search.tsx
│       │   └── settings.tsx
│       └── src/
│           ├── api/         # Axios API clients
│           ├── components/  # Shared UI components
│           ├── hooks/       # useLocation, usePlaces, useAuth
│           ├── store/       # Zustand stores
│           ├── types/       # TypeScript interfaces
│           └── utils/       # geocoding, routing, formatters
└── backend/                 # FastAPI Python backend
    ├── app/
    │   ├── models/          # SQLAlchemy ORM
    │   ├── schemas/         # Pydantic schemas
    │   ├── routers/         # API endpoints
    │   ├── services/        # Business logic
    │   └── tasks/           # Celery background jobs
    └── alembic/             # DB migrations
```

---

## API Endpoints

| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | `/api/auth/register` | Register |
| POST | `/api/auth/login` | Login → JWT tokens |
| POST | `/api/auth/refresh` | Refresh access token |
| GET  | `/api/places?lat=&lng=` | Nearby places |
| GET  | `/api/places/search?q=` | Search places |
| GET  | `/api/places/{id}` | Place detail |
| POST | `/api/places` | Add place (auth) |
| POST | `/api/verifications` | Verify status (auth) |
| GET  | `/api/users/me` | My profile |
| GET  | `/api/cities` | All cities |

---

## Environment Variables

Copy `.env.example` → `backend/.env` and fill in:

- `SECRET_KEY` — min 32 chars, keep secret
- `R2_*` — Cloudflare R2 for image uploads (optional for MVP)
- `FIREBASE_CREDENTIALS_PATH` — for push notifications (optional)

---

## Contributing

1. Fork the repo
2. Create a feature branch
3. Submit a PR

Issues welcome for bugs, new cities, or feature ideas.

---

## License

MIT — free for everyone, forever. 🌙
